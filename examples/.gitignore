# Generated by scenario `run.py` scripts on every invocation.
# .tmp/ holds the per-run SQLite database, WAL/SHM sidecars, and the
# rendered-report directory. None of it is portable across runs.
*/.tmp/
